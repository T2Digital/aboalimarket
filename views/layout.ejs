<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'لوحة الإدارة' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #1a1a2e 0%, #2a2a4a 100%);
            color: #ffffff;
            font-family: 'Tajawal', 'Arial', sans-serif;
            margin: 0;
            padding: 0;
        }
        .navbar {
            background: #1a1a3d !important;
            box-shadow: 0 0 10px #00ffcc;
            padding: 10px 0; /* تقليل padding النافبار */
        }
        .navbar-brand {
            display: flex;
            align-items: center;
            color: #00ffcc !important;
            font-size: 1.5rem;
            transition: all 0.3s ease;
        }
        .navbar-brand img {
            height: 40px; /* مقاس اللوجو */
            margin-left: 10px;
        }
        .nav-link {
            color: #00ffcc !important;
            transition: all 0.3s ease;
        }
        .nav-link:hover {
            color: #ff00ff !important;
            text-shadow: 0 0 5px #ff00ff;
        }
        .container {
            background: rgba(20, 20, 40, 0.9);
            border-radius: 10px;
            padding: 15px; /* تقليل padding */
            margin-top: 15px; /* تقليل margin */
            box-shadow: 0 0 15px rgba(0, 255, 204, 0.3);
            min-height: auto; /* إزالة min-height عشان المساحة الفاضية */
        }
        h2 {
            color: #00ffcc;
            text-shadow: 0 0 5px #00ffcc;
            margin: 20px 0; /* تقليل المسافة حوالين العنوان */
            text-align: right; /* محاذاة العنوان لليمين */
        }
        .table {
            border: 2px solid #00ffcc;
            box-shadow: 0 0 10px #00ffcc;
            background: #2a2a4a;
            border-radius: 8px;
            color: #ffffff;
            margin-top: 10px; /* تقليل المسافة قبل الجدول */
        }
        .table th, .table td {
            border: 1px solid #00ffcc;
            padding: 8px;
            text-align: center;
        }
        .table th {
            background: #1a1a3d;
            color: #00ffcc;
        }
        .btn-primary, .btn-secondary {
            border: 2px solid #ff00ff;
            background: #1a1a3d;
            color: #ffffff;
            box-shadow: 0 0 5px #ff00ff;
            transition: all 0.3s ease;
        }
        .btn-primary:hover, .btn-secondary:hover {
            box-shadow: 0 0 10px #ff00ff;
            transform: translateY(-2px);
        }
        .form-control, .form-select {
            border: 2px solid #00ffcc;
            background: #2a2a4a;
            color: #ffffff;
            box-shadow: 0 0 5px #00ffcc;
        }
        .form-control:focus, .form-select:focus {
            box-shadow: 0 0 10px #00ffcc;
            border-color: #00ffcc;
        }
        @media (max-width: 768px) {
            .navbar {
                padding: 8px 0;
            }
            .navbar-brand img {
                height: 30px;
            }
            .container {
                padding: 10px;
                margin-top: 10px;
            }
            .table th, .table td {
                padding: 6px;
                font-size: 14px;
            }
            .btn, .form-control, .form-select {
                font-size: 14px;
            }
            h2 {
                font-size: 1.5rem;
                margin: 15px 0;
            }
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin">
                <img src="/images/logo.png" alt="لوجو المتجر">
                لوحة الإدارة
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/admin">الرئيسية</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/update-store">تحديث المتجر</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/sales">إحصائيات المبيعات</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/admin/orders">متابعة الطلبات</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">تسجيل الخروج</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        <!-- المحتوى الفرعي هيترمي هنا من القوالب الفرعية -->
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>